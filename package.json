{
  "name": "hono-recipe",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test": "bun test --timeout 12000"
  },
  "dependencies": {
    "@google/generative-ai": "^0.3.0",
    "hono": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
